The key differences from the Claude-assisted template:

All tasks forced to model:local
.orchid.yaml explicitly sets all agents to local
Task design guidance tuned for local model strengths/weaknesses
Smaller, more focused tasks to avoid the multi-file code quality issues we saw in the orchid-dashboard run
--offline flag called out explicitly